<?xml version="1.0" encoding="utf-8"?>

<!--
    配置文件关键说明：
    应用正式发布之时，必须
    （1）核对[ga_trackingId]
    （2）置[ga_dryRun=false]、[ga_logLevel=error]
-->

<resources xmlns:tools="http://schemas.android.com/tools" tools:ignore="TypographyDashes">

    <!-- The apps Analytics Tracking Id -->
    <string name="ga_trackingId">UA-******</string>

    <!-- the Application Version of App. Default as manifest version code -->
    <!--<string name="ga_appVersion">Version 1.0.0</string>-->

    <!-- the Application Name for Analytics -->
    <string name="ga_appName">myapp</string>

    <!-- the Local LogLevel for Analytics -->
    <string name="ga_logLevel">error</string>
    <bool name="ga_reportUncaughtExceptions">true</bool>

    <!-- how often the dispatcher should fire -->
    <integer name="ga_dispatchPeriod">30</integer>

    <!-- Treat events as test events and don't send to google -->
    <bool name="ga_dryRun">false</bool>

    <!-- If ga_autoActivityTracking is enabled, an alternate screen name can be specified to
    substitute for the full length canonical Activity name in screen view hit. In order to
    specify an alternate screen name use an <screenName> element, with the name attribute
    specifying the canonical name, and the value the alias to use instead. -->
    <screenName name="com.example.myapp">myapp - ScreenName</screenName>

</resources>